function Projects({ isNavOpen }) {
	return (
		<div
			className={"full-stack-projects h-[46%]" + (isNavOpen ? " hidden" : "")}
		>
			<h2 className="mt-1">Full-stack Projects</h2>
			<section className=""></section>
		</div>
	);
}

export default Projects;
